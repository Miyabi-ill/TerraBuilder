<Window x:Class="TerraBuilder.Views.AddTextItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TerraBuilder.Views"
        mc:Ignorable="d"
        Title="AddTextItemWindow" Height="120" Width="400"
		x:Name="AddTextWindow"
		DataContext="{Binding ElementName=AddTextWindow}">
    <Grid>
		<DockPanel Margin="5">
			<Button Width="150" Margin="5" DockPanel.Dock="Bottom" HorizontalAlignment="Right" Click="Button_Click">追加</Button>
			<TextBlock Text="{Binding Description}" DockPanel.Dock="Top"></TextBlock>
			<TextBox Text="{Binding Text}" DockPanel.Dock="Top"></TextBox>
		</DockPanel>
    </Grid>
</Window>
